import { StyledInputCheckboxSlider } from './InputCheckboxSlider.styled';

const InputCheckboxSlider = (props) => {
  return (
    <StyledInputCheckboxSlider>{props.children}</StyledInputCheckboxSlider>
  );
};

export default InputCheckboxSlider;
