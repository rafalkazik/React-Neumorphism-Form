import { StyledIntroPageText } from './IntroPageText.styled';

const IntroPageText = (props) => {
  return <StyledIntroPageText>{props.children}</StyledIntroPageText>;
};

export default IntroPageText;
